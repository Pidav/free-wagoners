<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h2>Editer mon profil</h2>

      <%= simple_form_for [@user] do |f| %>
        <%= f.input :first_name %>
        <%= f.input :last_name %>
        <%= f.input :company_name %>
        <%= f.input :photo, as: :file %>
        <div class="row justify-content-center">
        <%= f.button :submit, class: "btn btn-primary rounded-pill" %>
      </div>
      <% end %>
    </div>
  </div>
<div>
